# 提交与 PR 规范

## 1. 提交信息
- 建议格式：`type(scope): summary`
- 示例：`feat(login): 新增登录页面与状态流转`

## 2. PR 必填项
- 变更目标与影响范围。
- 验证步骤与结果（至少包含 `flutter analyze`，有测试则含 `flutter test`）。
- UI 变更截图或录屏（如适用）。
- 已知问题与后续计划（如适用）。

## 3. 评审关注点
- 是否遵守分层职责，避免跨层耦合。
- 是否存在过深嵌套、超长函数、超大文件。
- 是否补充关键边界测试与错误处理。
- 文档是否同步更新。
